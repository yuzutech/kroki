# Code To UML for VS Code

Use **Code To UML: Sign in with Browser** to connect through the device authorization flow, or **Code To UML: Set API Key** to paste a personal key. Tokens are stored in VS Code SecretStorage, not settings.json.

Run **Code To UML: Preview Current Diagram** from the Command Palette to render the active editor beside your code.
